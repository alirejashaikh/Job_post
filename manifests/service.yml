apiVersion: v1
kind: Service
metadata:
    name: "shoppingcartaks-3382"
    labels:
        app: "shoppingcartaks-3382"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "shoppingcartaks-3382"