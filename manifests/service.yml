apiVersion: v1
kind: Service
metadata:
    name: "shoppingcartaks-3129"
    labels:
        app: "shoppingcartaks-3129"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "shoppingcartaks-3129"