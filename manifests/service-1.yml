apiVersion: v1
kind: Service
metadata:
    name: "shoppingcartaks-5ce6"
    labels:
        app: "shoppingcartaks-5ce6"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "shoppingcartaks-5ce6"